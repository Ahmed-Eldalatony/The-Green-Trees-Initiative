---
import { Image } from "astro:assets"
---
<section class="hero-section container">
  <Image class="cover-img" src="/images/Eating boy.png" alt="boy eating" width="1000" height="1000" />
  <div>
    <div class="content-container flex">
      <div class="card card--wide card--hero">
        <div class="mark inria-16">the issue</div>
        <h1 class="header-primary playfair-44">
          Climate change poses the biggest threat to women in developing
          nations.
        </h1>
        <p>
          <a class="inria-16" href="#"
            >Read more about this environmental and social cause.</a
          >
        </p>
      </div>
      <p class="tag flod-72">#stop climate change</p>
    </div>
  </div>
</section>
<style>
  .hero-section {
    position: relative;
  }
img{
position: absolute;
right: 0rem;
top: 0;
}
  .hero-section .card {
    height: 41.6rem;
  }

  .content-container {
    flex-direction: row;
    justify-content: space-between;
  }

  /* Responsive */
.hero-section {
  margin-inline: auto;
      /* width: 144rem; */
    }
  @media (max-width: 1300px) {
    .content-container {
      gap: 5rem;
    }
.hero-section {
      /* width: 110rem; */
    }
  }

  @media (max-width: 1100px) {
    .hero-section {
      /* width: 110rem; */
    }
  }
  @media (max-width: 768px) {
    .content-container {
      flex-direction: column;
      gap: 9.6rem;
    }
    .hero-section .mark {
      left: 6.4rem;
      transform: translate(0);
    }

    .content-container .tag {
      order: -1;
    }
  }

  @media (max-width: 576px) {
    .hero-section .mark {
      left: 50%;
      transform: translate(-50%);
    }
  }
  .cover-img.img {
    /* width: 100vh !important; */
  }
</style>
